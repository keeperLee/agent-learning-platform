/* ============================================================
   访问模式开关（单一来源）
   ------------------------------------------------------------
   这是「要不要登录」的唯一开关。

     open: true   —— 公开访问（当前设置）
       不校验登录，任何人打开即可阅读全部课程。
       适用于 GitHub Pages 这类没有服务端的静态托管。

     open: false  —— 恢复登录门禁
       启动时会请求账号服务 /api/session，由门户 ai-space 的
       server/auth.mjs 统一提供（本项目自身不再维护账号数据库）。
       需要 Node 运行环境。

   【为什么做成开关，而不是把认证删掉】
   账号、用户管理、scrypt 哈希这些代码都完整保留着。
   改回 false 即可恢复，不必重新开发。

   ⚠️ 本文件在 ai-space 门户仓库里有一份对应的副本
      （ai-space/assets/js/access.js）。两边要一起改，
      否则会出现「门户放开了但学习项目还在拦」。
   ============================================================ */

export const ACCESS = {
  open: true,

  /* 公开访问时是否还显示右上角的账号入口。
     默认关闭：没有登录体系时留一个点了没反应的按钮只会让人困惑。 */
  showAccountUI: false
};
