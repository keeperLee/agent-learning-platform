name: Server deployment required
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SQLite authentication requires a Node server and persistent disk. GitHub Pages publishing is disabled."
